# pip requirements for NP4Buoyancy (alternative to installation/environment.yml)
#
#   pip install -r installation/requirements.txt   # run from the repository root
#
# For a specific CUDA build of PyTorch, install torch first from the official
# index, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# then run the line above for the remaining packages.

torch>=2.3        # dist.init_process_group(device_id=...) requires >= 2.3
numpy>=1.23
scipy>=1.9
pyyaml>=6.0
matplotlib>=3.6

# optional: to run notebooks/NP4Buoyancy_intro.ipynb
jupyterlab
ipykernel
